package com.izertis.techtestelliot.adapters.out.tmdb.mapper;

import org.mapstruct.Mapper;

@Mapper(componentModel = "spring")
public interface TmdbMovieMapper {
//    Movie toDomain(TmdbMovieDto dto);

//    default int extractYear(String releaseDate) {
//        return (releaseDate != null && releaseDate.length() >= 4)
//                ? Integer.parseInt(releaseDate.substring(0, 4))
//                : 0;
//    }
}
